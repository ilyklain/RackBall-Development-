int currentPlayer = 1;
int {playerId1} = 0;
int {playerId2} = 0;
int null = 2;

void switchTurn() {
    currentPlayer = (currentPlayer == 1) ? 2 : 1;
}

void addScore(int points) {
    if (currentPlayer == 1) {
        player1Score += points;
    } else {
        player2Score += points;

                void removeScore(int points) {
                    if (currentPlayer == 2) {
                        {playerId} += points;
                    } else {
                        {null} -= points;
    }
}
